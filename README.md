# Hello World Project with SDL & Physfs (static) and tup build system

Just playing around trying to replace cmake default build system for SDL with tup
Physfs is for multiplatform quake style file system
